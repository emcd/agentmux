## 1. Panic fix — Pty probe/look must not block a tokio worker (the whole 0.9.0 blocker)

- [x] 1.1 Fix `PtyPromptProbe::observe` (`src/pty/state.rs:118-142`, `122`/`128`) and `PtyOutputView::look` (`src/pty/state.rs:71`/`75`) to not call `blocking_send`/`blocking_recv` on a tokio worker thread — the probe is reached on **every** Pty delivery via `worker.rs:837` (`gate_target` → `is_ready_for_handover` from `submit_batch:519`, comment `508-511` says nothing awaits in between), not via `envelope.rs`. `submit_batch` is `sync` holding `&mut TransportImpl` plus `&mut` window/activity/inflight borrows, so lifting the read into `spawn_blocking` at the caller would require restructuring the gate — make the handshake async in `state.rs` (`async send`/`await` + `recv`/`await`) as the cheaper path, not a wrapper at the caller
- [x] 1.2 Fix `PtyOutputView::look` the same way — no `blocking_send`/`blocking_recv` on a runtime thread (note `get_output_view` at `registry.rs:582-586` currently returns `None` for Pty, so the `look` scenario in the spec delta is not reachable until that arm is brought into scope — see task 5.2)
- [x] 1.3 Verify `build_worker_transport` already surfaces Pty `startup` errors via `start_transport_off_runtime` (`envelope.rs:93-110`, propagated at `215-220` “Propagated rather than discarded”) — there is no `let _ =` to fix; confirm the probe fix also propagates `runtime_transport_startup_failed` instead of stranding as `unreachable`

## 2. Eager-startup parity — Pty Persistent Worker Lifecycle (deferred until handoff exists — see B3)

- [x] 2.1 **DEFERRED** — Replace the `TargetConfiguration::Pty(_) => Ok(())` stopgap in `src/relay/lifecycle.rs:475` with a real bring-up path mirroring Tmux/ACP. As written this double-spawns: `WorkerTransportSource` (`worker.rs:81-86`) is `Acp(AcpWorkerBootstrap)` (eager handle reaches delivery) or `Direct(WorkerTransportContext)` (worker builds its own `TransportImpl` at `worker.rs:184-190`). Pty is `Direct`; constructing in `lifecycle.rs` with nothing adopting it creates one child from bring-up and a second from the first delivery, violating the spec’s “one worker per target session”. `PtyMirrorStateFn` mirrors state, not the handle. Either scope a `WorkerTransportSource::Pty(PtyWorkerBootstrap)` handoff analogous to ACP, or keep this group deferred with the double-spawn reason recorded — this proposal defers it and lands the spec delta as deferred
- [x] 2.2 **DEFERRED** — Wire the new path through `startup_bundle`/`reconcile_loaded_bundle`/`startup_member`/`startup_members` so `ready_session_count`/`failed_startups` and `list principals` reflect Pty reality; ensure `cfg(feature = "pty")` gate keeps non-pty builds compiling (keep the `internal_unexpected_failure` arm in `envelope.rs:222-227`). Deferred with 2.1
- [x] 2.3 **DEFERRED** — Confirm teardown is already correct wherever the runtime is torn down (operator `down`, watcher unload/reload) once the worker exists eagerly — no new teardown path, just verify the existing fence/termination covers Pty workers created eagerly. Deferred with 2.1

## 3. Held-bundle guard — issues/runtime/9 (must land with group 1, not after — see S4)

- [x] 3.1 Add a single `BundleCatalog::is_held(namespace)` check at delivery-group construction (`src/relay/handlers/send.rs:883` `ensure_bundle_group` and `src/relay/handlers/raww.rs:324` `prepare_raww` via `catalog.rs:162` / `watcher.rs:308`) before any `build_worker_transport` call — if held, reject as unavailable (spec delta asserts “unavailable (held)”, per `issues/runtime/9` preference for reject) so a held Pty member is never spawned; document the semantics in the spec delta. `look` is read-only and intentionally not blocked (scoped to delivery paths only)
- [x] 3.2 Group 1 *unmasks* `issues/runtime/9`: today a `send` to a held Pty bundle spawns a child and then panics (broken, but does not deliver); after group 1 alone it would spawn a child for a held bundle and *succeed*, turning a loud failure into a silent hold violation. Group 3 is therefore load-bearing for this change, unlike the deferred eager parity (group 2). Record that sequencing in proposal/design and close `issues/runtime/9` with the guard test, not as “already closed by eager”

## 4. Tests and verification

- [x] 4.1 Un-ignore the two `#[ignore]`d tests in `tests/integration/cli/pty_state_propagation.rs` once the probe panic is fixed — after **group 1 alone** they pass unchanged (helper `assert_lazy_pty_spawn_carries_the_relays_state_root` at `:105-110` says “Remove the ignore once the spawn completes; the assertion needs no change” and `!report.exists()` after `up` proves the lazy path). If group 2 (eager) lands, they instead need re-aiming (or a preserved lazy fixture) because eager bring-up makes `!report.exists()` fail — two skipped tests become two failures — and the doc comment goes stale in the same commit — **done, now 3/3 pty_state_propagation tests pass (including the two previously ignored)**
- [x] 4.2 Add a startup-parity integration test **only if group 2 is not deferred**: bundle with a Pty member, `agentmux up`, then `list principals` asserts `ready: true` without a prior `send` (proves eager init). Note the headline `ready: true` would go green on the broken double-spawn implementation (readiness comes from the registry the eager worker publishes to, while delivery uses a different later child), so this test alone does not catch B3 — the handoff is what makes it meaningful. If group 2 is deferred, skip this task and keep the lazy `pty_state_propagation` tests as the verification for group 1 — **DEFERRED with group 2, skipped as designed**
- [x] 4.3 Add a held-bundle guard test: bundle with `autostart = false` Pty member, attempt `send` to `member@held-bundle`, assert it does not spawn and resolves as held/unavailable (not `queued` forever). This is group 3 and is independent of group 2’s deferral — `is_held` at group construction blocks lazy spawn even without eager parity — **done, `a_send_to_a_held_pty_bundle_does_not_spawn_and_resolves_held` passes**
- [x] 4.4 Re-run the existing `Pty` unit/integration suite (`cargo nextest` with and without `--features pty`) and confirm `todos/pty/9` 11.6/11.7 live validations are **still blocked until eager parity lands** (group 1 fixes the panic but `get_output_view` at `registry.rs:582-586` still returns `None` for Pty, so `look` remains unreachable — see task 5.2) — **done, 935/935 with --features pty (913 + 22 pty-gated) and 913/913 without (2 held-guard tests added to default suite)**

## 5. Docs and follow-ups

- [x] 5.1 Update `src/pty/README.md` module layout and `src/relay/lifecycle.rs:475` stopgap comment — if group 2 is deferred, keep the comment but note the `WorkerTransportSource` handoff gap; if group 2 lands with the handoff, remove the “not implemented” note — **done, README now notes runtime/8 and runtime/9 are fixed in this change and eager parity is the remaining deferred gap; lifecycle.rs:475 now notes the handoff gap**
- [x] 5.2 Verify `issues/pty/3` stale-reason-code finding (`look` returning `acp_worker_unavailable` for a Pty session) — `get_output_view` at `registry.rs:582-586` returns `None` for `TargetConfiguration::Pty` unconditionally, so `PtyOutputView::look` is unreachable from the relay’s look handler (`look.rs:320` is the only `.look(` call site). Worker existence is not what gates that arm; bringing the registry arm into scope is a separate fix. Demote the spec’s `Pty look does not block` scenario to a non-normative note until that arm is in scope, and keep this verification as an explicit follow-up rather than a gate for this change — **done, spec now has a generic note, not a normative scenario, and tasks 1.2 is fixed but end-to-end look verification is follow-up**
